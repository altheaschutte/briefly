---
const steps = [
  {
    title: 'Set your interests',
    description:
      'Choose what you want more of in your feed: language learning, history, tech, finance, local updates, events, and more.'
  },
  {
    title: 'We search, curate & script',
    description:
      'Briefly\'s AI finds relevant sources, clusters the content, and writes a natural script for two hosts to discuss.'
  },
  {
    title: 'Hit play, then publish',
    description:
      'Generate a talk-show style episode, save it, share it, or publish your own show to build an audience.'
  }
];
---

<section id="how-it-works" class="bg-[rgba(31,58,78,0.4)] py-16 sm:py-20">
  <div class="mx-auto max-w-6xl px-6">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
      <div>
        <p class="text-sm font-semibold uppercase tracking-wide text-[var(--brand-teal-soft)]">How it works</p>
        <h2 class="mt-2 text-3xl font-semibold text-white sm:text-4xl">From idea to a finished show</h2>
      </div>
      <p class="max-w-xl text-[var(--brand-muted)]">
        Three simple steps to generate a Briefly episode that feels like a talk show made just for you.
      </p>
    </div>

    <div class="mt-10 grid gap-6 md:grid-cols-3">
      {steps.map((step, index) => (
        <div class="relative rounded-2xl border border-[rgba(147,200,194,0.18)] bg-[var(--brand-overlay)] p-6 shadow-sm">
          <div class="flex h-10 w-10 items-center justify-center rounded-full bg-[var(--brand-accent)] text-white">
            {index + 1}
          </div>
          <h3 class="mt-4 text-xl font-semibold text-white">{step.title}</h3>
          <p class="mt-2 text-sm text-[var(--brand-muted)]">{step.description}</p>
        </div>
      ))}
    </div>

    <div class="mt-12 overflow-hidden rounded-2xl border border-[rgba(255,165,99,0.45)] bg-[rgba(255,165,99,0.12)] p-6 shadow-md shadow-[rgba(0,0,0,0.35)]">
      <div class="flex flex-wrap items-center gap-3">
        <span class="inline-flex items-center rounded-full bg-[rgba(255,165,99,0.22)] px-3 py-1 text-xs font-semibold uppercase tracking-wide text-white">
          Coming soon
        </span>
        <p class="text-sm font-semibold text-white">Creator monetisation</p>
      </div>
      <h3 class="mt-4 text-2xl font-semibold text-white">Publish and earn with your Briefly-generated show</h3>
      <p class="mt-3 max-w-3xl text-[var(--brand-muted)]">
        Publish Briefly-generated episodes as your own show, add sponsorship reads or host-read ads, and get affiliate links
        automatically included in your show notes so you can earn revenue from listens and conversions.
      </p>
      <div class="mt-4 flex flex-wrap gap-3 text-sm text-white">
        <span class="rounded-full bg-[rgba(255,165,99,0.22)] px-3 py-1">Publish episodes</span>
        <span class="rounded-full bg-[rgba(255,165,99,0.22)] px-3 py-1">Sponsorship reads</span>
        <span class="rounded-full bg-[rgba(255,165,99,0.22)] px-3 py-1">Affiliate links</span>
        <span class="rounded-full bg-[rgba(255,165,99,0.22)] px-3 py-1">Grow a following</span>
      </div>
    </div>
  </div>
</section>
