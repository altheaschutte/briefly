---
import Layout from '../components/Layout.astro';
import { siteDescription, supportEmail } from '../config/site';

const title = 'Support & Contact | Briefly';
---

<Layout title={title} description={siteDescription}>
  <section class="mx-auto max-w-3xl px-6 py-16 sm:py-20">
    <div class="space-y-3">
      <p class="text-sm font-semibold uppercase tracking-wide text-[var(--brand-teal-soft)]">Support</p>
      <h1 class="text-3xl font-semibold text-white sm:text-4xl">Support & Contact</h1>
      <p class="text-[var(--brand-muted)]">Briefly is in early access; support is currently email-based.</p>
      <a
        href={`mailto:${supportEmail}`}
        class="inline-flex items-center gap-2 rounded-full border border-[rgba(147,200,194,0.22)] bg-[var(--brand-accent)] px-4 py-3 text-sm font-semibold text-white shadow-sm shadow-[rgba(255,165,99,0.25)] transition hover:-translate-y-0.5 hover:bg-[#ffb67f]"
      >
        Email us at {supportEmail}
      </a>
    </div>

    <div class="mt-10 space-y-6">
      <h2 class="text-xl font-semibold text-white">FAQ</h2>
      <div class="space-y-4">
        <div class="rounded-2xl border border-[rgba(147,200,194,0.18)] bg-[rgba(13,30,43,0.7)] p-4">
          <p class="text-sm font-semibold text-white">When is the app launching?</p>
          <p class="mt-1 text-sm text-[var(--brand-muted)]">
            We're onboarding users gradually during early access. Join the waitlist to get notified as soon as your invite
            is ready.
          </p>
        </div>
        <div class="rounded-2xl border border-[rgba(147,200,194,0.18)] bg-[rgba(13,30,43,0.7)] p-4">
          <p class="text-sm font-semibold text-white">Which platforms will Briefly support?</p>
          <p class="mt-1 text-sm text-[var(--brand-muted)]">Mobile and web listening are first, with podcast feeds for your player coming soon.</p>
        </div>
        <div class="rounded-2xl border border-[rgba(147,200,194,0.18)] bg-[rgba(13,30,43,0.7)] p-4">
          <p class="text-sm font-semibold text-white">How will creator payouts work?</p>
          <p class="mt-1 text-sm text-[var(--brand-muted)]">
            Sponsorship reads and affiliate links are baked into episodes. Payouts will be tracked based on listens and
            conversions with transparent dashboards for creators.
          </p>
        </div>
        <div class="rounded-2xl border border-[rgba(147,200,194,0.18)] bg-[rgba(13,30,43,0.7)] p-4">
          <p class="text-sm font-semibold text-white">Can I use my own voice or brand?</p>
          <p class="mt-1 text-sm text-[var(--brand-muted)]">
            Yes. Bring your brand style and host voices; we'll generate scripts and sound to match your tone.
          </p>
        </div>
      </div>
    </div>
  </section>
</Layout>
